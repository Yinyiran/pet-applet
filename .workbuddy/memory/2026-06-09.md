# 2026-06-09 工作日志

- 订单详情弹窗增加价格明细：商品原价、优惠减免（红色）、实付金额、支付方式明细（余额/微信/支付宝）
- profileOrders 数据结构增加 originalTotal、discount、payment 字段
- 新增 CSS 类：.order-detail-discount、.order-detail-pay-breakdown、.order-detail-pay-item
- **会员等级轮播图交互逻辑完成**：
  - `js/profile.js` 实现 `switchMemberLevel()`/`goToMemberLevel()`/`updateMemberLevelDisplay()`
  - 左右按钮切换 + 圆点指示器点击 + 滚动动画
  - 顶部当前会员信息始终显示真实等级，不随轮播切换改变
  - 进度条根据累计消费动态计算升级进度
  - 修复 profile.js 第151行孤立重复代码语法错误
- **会员权益整合到个人详情弹窗**：
  - 从"我的"页面移除"会员信息"菜单项
  - 将会员权益内容（当前等级信息 + 三级轮播图 + 进度条）完整移入 `profileDetailOverlay`
  - `openProfileDetail()` 增加轮播初始化逻辑（设置索引 + 调用 updateMemberLevelDisplay）
  - 个人详情弹窗 max-height 从 60vh 调整为 80vh 以容纳更多内容
  - 原 `memberDetailOverlay` 保留在 HTML 中暂未删除（可后续清理）
