# 2026-06-07 工作日志

## 修改 commission-tier-card 样式

- 修改了 `commission.css` 文件
- 将 `.commission-tier-card` 从旧的多行布局改为新的两行布局
- 删除了旧的CSS类：`.commission-tier-badge`、`.commission-tier-name`、`.commission-tier-label`、`.commission-tier-rate-item`、`.commission-tier-rate-pct`、`.commission-tier-rate-label`
- 添加新的CSS类用于两行布局：
  - `.commission-tier-main`: 主标题行容器（flex布局，左右分布）
  - `.commission-tier-type`: 类型名称样式（16px，粗体）
  - `.commission-tier-percentage`: 提佣百分比样式（24px，粗体，主题色）
  - `.commission-tier-desc`: 副标题说明样式（12px，辅助色）
- 卡片 padding 从 `16px` 调整为 `14px 16px`

## 个人信息编辑增加手机号字段

- 修改 `index.html`：在个人信息编辑弹窗中添加手机号输入框（第1879行）
  - 添加 `<input type="tel" id="profilePhoneInput" placeholder="请输入手机号" maxlength="11" />`
- 修改 `js/profile.js`：
  - `openProfileEdit()` 函数：添加加载手机号逻辑（第88行）
  - `saveProfile()` 函数：添加保存手机号逻辑（第100行）
- `userProfile` 对象已包含 `phone` 字段（15677778888）

## 删除我的团队中的邀请码和复制邀请码

- 删除 `index.html` 中邀请码显示区域 (`inviteShareCode`) 和"复制邀请码"按钮
- 删除 `js/commission.js` 中 `copyInviteCode()` 函数及 `renderInviteDetail()` 中的邀请码生成逻辑
- 删除 `commission.css` 中 `.invite-share-code` 样式

## 拆分"邀请好友"为独立模块并重构我的团队弹窗

- 在 `index.html` 我的页面添加"邀请好友"独立菜单项（带 `profileInviteBadge` 徽章），位于积分与分佣收益之间
- 从分佣概览弹窗中移除"邀请好友"按钮
- 我的团队弹窗重构：
  - 添加 Tab 切换（邀请总数 / 浏览量），复用 `.commission-detail-tabs` 样式，默认选中第一个
  - 删除"贡献收益"统计卡片，改为 2 列显示（邀请总数 + 浏览量）
  - 人员列表：级别从"一级/二级/三级"改为"用户/梵优合伙人/梵优主理人"角色标签
  - 列表项右侧显示浏览量（N），点击可进入用户详情弹窗
- 新增邀请用户详情弹窗（`#inviteUserOverlay`）：显示头像、用户名、角色、加入时间、状态、浏览量、最近浏览时间、贡献收益
- 更新 `js/commission.js`：
  - `commissionData` 新增 `totalViews` 字段
  - `inviteList` 数据模型重构：`level` → `role`，新增 `browseCount`、`browseTime` 字段
  - 新增 `switchInviteTab()`、`openInviteUserDetail()`、`closeInviteUserDetail()` 函数
  - `renderCommissionInfo()` 增加 `profileInviteBadge` 更新逻辑
  - `renderCommissionOverview()` 移除邀请徽章更新
- 更新 `commission.css`：
  - 删除 `.invite-item-contribution`、`.invite-item-level` 样式
  - 新增 `.invite-item-role`（含 `.partner`、`.manager` 变体）和 `.invite-item-views` 样式
  - 新增 `.invite-user-overlay`、`.invite-user-sheet`、`.invite-user-content`、`.invite-user-detail-list` 等用户详情弹窗样式
  - `.invite-item` 增加 `cursor: pointer` 和 `:active` 状态

## 合并邀请 Tab 与统计卡片

- 删除 `invite-stats` 独立统计卡片区域
- Tab 标签内直接显示数量：`邀请总数 35` | `浏览量 992`
- Tab 尺寸放大：`padding: 10px 8px`，`font-size: 14px`
- 数量使用独立 `invite-tab-count` 样式：`font-size: 16px`，`font-weight: 800`
- JS 适配新的 `.invite-tab` 类名（替代 `.commission-detail-tab`）
- CSS 新增 `.invite-tabs`、`.invite-tab`、`.invite-tab-count` 样式；注释保留旧 `.invite-stats` 样式

## 收益明细条目点击详情弹窗

- `commissionLog` 数据模型扩展：每条 order 类型记录新增 `orderAmount`（支付金额）、`rate`（返佣比例）、`items`（商品列表数组，含 name/price/qty）
- `renderRecentCommissionLog()` 和 `renderCommissionDetailList()`：为 `.commission-log-item` 添加 `onclick` 调用 `openCommissionLogDetail(id)`
- 新增 `openCommissionLogDetail(logId)` 函数：查找记录 → 填充弹窗（支付金额、收益公式、收益金额、状态、时间、商品列表）
- 新增 `closeCommissionLogDetail(e)` 函数
- `index.html` 新增收益详情弹窗 `#commissionLogDetailOverlay`：顶部金额区（绿色渐变背景，含公式行）、状态+时间栏、商品列表
- `commission.css` 新增 `.comm-log-detail-*` 系列样式（金额区、公式、状态标签、商品列表项）；`.commission-log-item` 增加 `cursor: pointer`
