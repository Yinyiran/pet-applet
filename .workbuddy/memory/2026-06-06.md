# 2026-06-06

## 同城卡片 - 导航和电话功能

**需求（v1）**：在 city-card 的 `.city-card-addr` 行右侧增加导航图标和电话文字。

**需求（v2）**：导航图标放到 `.city-card-addr` 地址后面，电话放到 `.city-card-tags` 评分后面（图标+号码）。

### 修改内容

1. **cityData 数据**：为每个商家添加 `tel` 字段（电话号码）

2. **JS (`js/city.js`)**：
   - `.city-card-tags` 恢复简单标签布局
   - `.city-card-addr` 改为两段式：左侧地址（SVG定位图标 + 文字），右侧导航图标 + 电话文字
   - 导航使用 `navigateTo(idx)` 函数，调用高德地图 URI Scheme 按地址搜索
   - 电话使用 `copyTel(tel)` 函数，优先 Clipboard API，降级用 `execCommand('copy')`
   - 两个操作都通过 `event.stopPropagation()` 阻止冒泡，不触发卡片点击

3. **CSS (`style.css`)**：
   - `.city-card-tags` 恢复原始 flex-wrap 布局
   - 删除之前添加的 `.city-card-tags-left/right`、`.city-card-action` 样式
   - `.city-card-addr` 改为 `justify-content: space-between`
   - 新增 `.city-card-addr-left`（左侧，flex:1 + 文字截断）和 `.city-card-addr-right`（右侧，flex-shrink:0）
   - 新增 `.city-card-nav`（导航按钮，22px圆形，主色）和 `.city-card-tel`（电话文字，主色加粗，点击复制）

## 抖音直播间介绍卡片

**需求**：点击「直播中」按钮时弹出卡片，展示抖音直播间介绍（着重 AI 定制配餐），底部显示抖音号+复制。

### 修改内容

1. **`index.html`**：
   - 新增 `.douyin-overlay` + `.douyin-card` 弹窗结构
   - 卡片 hero 区（渐黑底+直播中 badge+标题）
   - 三个介绍 section：AI 定制配餐、直播间福利、如何参与（title+desc 使用 `.douyin-card-section-info` 包裹实现上下结构，icon 在左）
   - 底部 footer：抖音号 `FanYouMingChong` + 复制按钮
   - 抖音按钮 `onclick` 改为 `openDouyinCard()`

2. **`style.css`**：追加全套 `.douyin-*` 样式，与现有弹窗风格一致（slideUp 动画、圆角、handle 条）

3. **`app.js`**：新增 4 个函数
   - `openDouyinCard()` — 显示弹窗
   - `closeDouyinCard(e)` — 点击遮罩关闭
   - `copyDouyinId(e)` — Clipboard API 复制抖音号
   - `fallbackCopyDouyinId(text)` — 降级复制方案

## AI答题页 - 逐题分页改造

**需求**：
1. 逐题显示（一次一题），单选题点击后自动跳转下一题
2. 底部「上一题」「下一题」按钮（下一题仅当前题答完后显示）
3. 最后一题答完才显示「提交，开始AI配餐」
4. 宠物类型选项显示大图标（52px emoji 卡片式）
5. 其他问题选项改为垂直排列（一行一个）
6. 优化 quiz-page-title/subtitle 样式

### 修改内容

1. **`index.html`**：
   - 所有 quiz-field 用 `<div id="quizQuestionsArea">` 包裹
   - 宠物类型选项改为 `.quiz-radio-pet` 结构（大 emoji + 标签）
   - 新增导航栏 `.quiz-nav`（上一题/指示器/下一题）
   - submit 区加 `id="quizSubmitWrap"` 并初始 `hidden`

2. **`style.css`**：
   - `.quiz-page-title`：26px 渐变橙色文字（-webkit-background-clip: text）
   - `.quiz-page-subtitle`：居中、max-width 280px、优化行高
   - `.quiz-radio-group` / `.quiz-checkbox-group`：改为 `flex-direction: column`（垂直）
   - `.quiz-radio-pet`：水平双卡片，52px 大图标，选中时缩放 1.1
   - 新增 `.quiz-questions-area`、`quizFadeSlideIn` 动画
   - 新增 `.quiz-nav`、`.quiz-nav-btn`（主色圆角按钮）、`.quiz-nav-indicator`

3. **`js/quiz.js`**（核心重写，保留海报/购买逻辑）：
   - 新增状态：`quizCurrentIndex`、`quizAutoAdvanceTimer`
   - `buildQuestionList()`：遍历 DOM 构建当前可见题目列表
   - `showQuestion(index)`：隐藏所有 → 显示当前 → 更新导航/进度
   - `goToNextQuestion()` / `goToPrevQuestion()`：手动导航
   - `autoAdvanceNext()`：350ms 延迟自动跳转（单选用）
   - `updateNavButtons()`：控制 prev/next/submit 显隐
   - `selectRadio()`：保留条件字段逻辑 + 新增 autoAdvanceNext()
   - `toggleCheckbox()` / `onQuizInput()`：新增 updateNavButtons()
   - `resetQuiz()`：清除定时器、恢复 display、隐藏导航/submit

## 配餐海报页 - poster-hero 体质检测分析卡片

**需求**：将 `poster-hero` 从装饰性背景图改造为展示 AI 体质分析结果的卡片（方案 B）。

### 修改内容

1. **`js/quiz.js`**：
   - 新增 `generateConstitutionAnalysis(answers, isCat)` 函数
   - 分别定义猫咪 6 个维度（情绪陪伴、肠胃、皮毛、泌尿、关节、过敏）和狗狗 7 个维度（运动活力、肠胃、皮肤、泪痕、关节、代谢、过敏）的分析规则
   - 每个维度包含：字段 key、标签 label、健康选项 healthy、体质标签 tag、emoji、各异常选项的文案 texts
   - 分析逻辑：统计各维度异常选项数量 → 问题最多维度定体质类型 → 生成个性化描述文案 → 列出关注维度标签
   - 特殊处理：全健康 →「健康活力型」；单一维度最多 → 该维度标签；多维度并列 → 复合型标签
   - `renderPoster()` 英雄区：调用分析函数，生成 badge + 描述 + 标签云，填充到 `#posterHeroContent`

2. **`index.html`**：
   - `poster-hero` 内新增 `.poster-hero-content` 内容层（id="posterHeroContent"）
   - 默认占位结构：constitution-header（badge + title）+ constitution-desc 
   - 删除旧的 `posterHeroTag` 元素

3. **`style.css`**：
   - `.poster-hero`：移除固定 `aspect-ratio`，改为自适应高度
   - `.poster-hero-img`：`position: absolute`，z-index 0，作为背景层
   - 背景 emoji 装饰：从居中改为右侧，opacity 0.18（弱化装饰性）
   - 新增 `.poster-hero-content`：relative z-index 1，padding 16px
   - 新增 `.constitution-header`：flex 横向排列 badge + title
   - 新增 `.constitution-badge`：白色半透明背景、圆角 pill、主色文字
   - 新增 `.constitution-title`：灰色小字
   - 新增 `.constitution-desc`：13px、1.65 行高、max-width 85%
   - 新增 `.constitution-tag`：白色半透明 pill、小字、emoji 前缀

## 配餐海报页 - 增加生成海报和分享功能

**需求**：在海报页底部购买栏增加「生成海报」和「分享」两个功能按钮。

### 修改内容

1. **`index.html`**：
   - 引入 html2canvas CDN（`https://cdnjs.cloudflare.com/ajax/libs/html2canvas/1.4.1/html2canvas.min.js`）
   - 改造 `.poster-buy-bar` 内部结构：左侧价格信息（flex:1），右侧三个按钮横排
   - 新增 `.poster-buy-actions` 按钮容器
   - 新增两个 outline 风格按钮：「📷 生成海报」「🔗 分享」
   - 「立即购买」按钮保留，改为 `.poster-action-primary`
   - 新增海报预览弹窗 `.poster-preview-overlay`：
     - 头部：标题「长按保存海报」+ 关闭按钮
     - 主体：居中显示生成的海报图片（`.poster-preview-img`）
     - 底部操作栏：「保存到相册」「分享给好友」两个按钮

2. **`style.css`**：
   - 调整 `.poster-buy-bar`：padding 缩小到 8px 12px，gap 缩小到 8px
   - 新增 `.poster-buy-actions`：flex 横排，gap 6px，flex-shrink:0
   - 新增 `.poster-action-btn` 基础样式：inline-flex、圆角、12px 字号、点击缩放动效
   - 新增 `.poster-action-outline`：白底 + 主色 1.5px 边框，点击反馈背景变色
   - 新增 `.poster-action-primary`：主色渐变背景 + 阴影（保持原购买按钮视觉）
   - 新增海报预览弹窗全套样式：
     - `.poster-preview-overlay`：fixed 全屏遮罩，z-index 1000
     - `.poster-preview-card`：白色圆角卡片，max-width 380px，max-height 88vh，slideUp 入场动画
     - `.poster-preview-header`：标题 + 圆形关闭按钮
     - `.poster-preview-body`：灰色背景，居中展示图片
     - `.poster-preview-actions`：两个按钮（outline + primary）

3. **`js/quiz.js`**（新增 5 个函数）：
   - `generatePosterImage()`：
     - 检查 html2canvas 是否加载
     - 临时隐藏底部栏 → 滚动到顶部 → 调用 html2canvas（scale:2 高清输出）
     - 恢复底部栏 → 将 canvas 转为 dataUrl → 填充到预览 img → 显示弹窗
   - `closePosterPreview(e)`：点击遮罩关闭弹窗（子元素冒泡不触发），清空 img.src
   - `dataUrlToBlob(dataUrl)`：辅助函数，将 base64 dataUrl 转为 Blob 对象
   - `downloadPosterImage()`：
     - 创建 a 标签，设置 download 属性和 dataUrl href
     - 临时添加到 body → click() → 移除（兼容所有浏览器）
     - Toast 提示「已保存海报图片」
   - `sharePoster()`（底部「分享」按钮点击）：
     - 如果图片未生成 → 调用 `generatePosterImage()`
     - 如果已生成 → 直接显示预览弹窗
   - `sharePosterImage()`（预览弹窗内「分享给好友」点击）：
     - 用 `dataUrlToBlob()` 将图片转为 Blob → 封装为 File 对象
     - 优先使用 `navigator.canShare({ files })` + `navigator.share({ files })` 分享图片文件
     - 不支持文件分享时降级到 `navigator.share({ url })` 分享链接
     - 完全不支持 Web Share API 时 Toast 提示「请长按海报图片分享」

### 技术细节

- **html2canvas 配置**：scale:2（高清）、useCORS:true、backgroundColor 匹配页面暖米色
- **Web Share API 降级策略**：文件分享 → 链接分享 → 提示长按保存
- **dataUrl 判断**：用 `startsWith('data:image')` 判断是否已生成海报图片（空字符串返回 false）
- **下载兼容**：a 标签必须临时添加到 DOM 并触发 click，否则某些浏览器不会下载
