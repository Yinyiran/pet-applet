# 2026-06-08 工作日志

## 邀请明细列表项(invite-item)改版

- **数据层**：inviteList 每条记录新增 `level`（等级）、`teamSize`（团队人数）、`hasConsumed`（是否消费）字段
- **布局重构为头像+两行**：
  - 左侧：头像（40px 圆形）
  - 右侧 `.invite-item-body` 两行：
    - 第一行：姓名（左） | 等级标签 + 团队人数（右）
    - 第二行：注册日期（左） | 消费状态标签（右）
- **删除的元素**：贡献金额(contribution)、浏览量、最近浏览
- **CSS**：删除 `.invite-item-avatar`/`.invite-item-info`/`.invite-item-right`/`.invite-item-contribution`/`.invite-item-views`，新增 `.invite-item-row`/`.invite-item-row-right`
- **用户详情弹窗**：同步修改，删除浏览量/最近浏览/贡献收益，新增等级/团队人数/消费状态
