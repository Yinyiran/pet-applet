# 2026-06-10

## shop-product-card 统一为单列横向布局

- **需求**: shop-list 中所有 shop-product-card 改为单列显示（同原 `--featured` 修饰符的横向布局）
- **CSS 修改** (`style.css`):
  - `.shop-product-card`: width → 100%, display: flex row, margin-bottom: 4px
  - `.shop-product-img-wrap`: 固定 140px 宽, min-height 140px（原 100% / 120px）
  - `.shop-product-img`: font-size 56px（原 48px）
  - `.shop-product-info`: flex column, space-between, padding 10px 12px（原上下紧凑布局）
  - `.shop-product-name`: 14px / 700（原 12px / 600）
  - 新增 `.shop-product-desc`: 11px, 2行截断
  - `.shop-product-meta`: 新增 margin-bottom: 6px
  - `.shop-product-price`: 18px（原 15px）
  - `.shop-product-cart-btn`: 28px（原 22px）, svg 14px（原 12px）
  - `.city-detail-products-grid .shop-product-card`: width 100%（原 50%）
- **JS 修改**:
  - `shop.js` `renderShopList()`: 移除首项特殊处理(idx===0)，所有卡片使用统一模板（含 desc + meta + bottom 三行布局）
  - `city.js` 同城详情卡片模板同步更新，新增 desc 行
- `--featured` 系列 CSS 变体保留未删（无引用，无副作用）

## 首页客服悬浮按钮（拖拽吸附四边）

- **需求**: 首页增加可拖拽客服悬浮按钮，松手自动吸附到最近边缘（上下左右四边）
- **HTML** (`index.html`):
  - `.app-container` 内、`</main>` 后、bottom-nav 前插入 `#csFloatBtn` 元素（50px 圆形，💬 图标 + 红色角标）
- **CSS** (`style.css`):
  - 新增 `.cs-float-btn` 系列样式：absolute 定位，z-index:150，橙色渐变背景
  - `.cs-float-btn.dragging`: 拖拽中取消 transition、增强阴影
  - `.cs-float-btn.snapping`: 松手后 spring easing 吸附动画（0.35s cubic-bezier）
  - `@keyframes csPulse`: 待机时呼吸灯效果
- **JS** (`js/app.js`):
  - IIFE `initCSFloatBtn()`: touch 事件驱动拖拽（touchstart/move/end）
  - 拖拽中 clamp 在 header 与 bottom-nav 之间安全区域
  - touchend 计算按钮中心点到四边距离，取最小值吸附
  - resize 事件重定位
  - `showCSButtonOnHome()`: 仅首页(homePage.active)显示，切换 tab 或返回时自动显隐
  - `openCustomerService()`: 点击触发 toast 提示

## 我的团队：搜索框替换为 level 等级筛选

- **需求**: 移除 invite-search 搜索框，改为按用户等级（level）筛选团队成员
- **HTML** (`index.html`):
  - 移除 `.invite-search` 搜索框（含放大镜图标 + input）
  - 新增 `.invite-level-filter#inviteLevelFilter`，5 个 chip 按钮：全部 / 普通用户 / 梵优合伙人 / 梵优主理人 / 分公司
- **JS** (`commission.js`):
  - 新增 `inviteLevelFilter` 变量（默认 'all'）
  - `openInviteDetail()`: 重置 `inviteLevelFilter = 'all'`
  - `renderInviteDetail()`: 新增等级筛选 chip 激活状态同步
  - `renderInviteList()`: 参数签名从 `(filter)` 改为 `()`，文本搜索逻辑替换为 `inviteLevelFilter !== 'all'` → `u.level === inviteLevelFilter`
  - 空状态文案: "该等级暂无团队成员" / "暂无邀请记录"
  - 移除 `searchInvite()`，新增 `filterInviteByLevel(level, el)`
- **CSS** (`commission.css`):
  - 移除 `.invite-search` / `.invite-search-icon` / `.invite-search-input` 样式块
  - 新增 `.invite-level-filter`（flex row + 水平滚动 + 隐藏滚动条）+ `.invite-level-chip`（圆角药丸，active 态橙色填充白色文字，按下 scale 0.96）
