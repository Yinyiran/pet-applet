# 2026-06-10

## shop-product-card 统一为单列横向布局

- **需求**: shop-list 中所有 shop-product-card 改为单列显示（同原 `--featured` 修饰符的横向布局）
- **CSS 修改** (`style.css`):
  - `.shop-product-card`: width → 100%, display: flex row, margin-bottom: 4px
  - `.shop-product-img-wrap`: 固定 140px 宽, min-height 140px（原 100% / 120px）
  - `.shop-product-img`: font-size 56px（原 48px）
  - `.shop-product-info`: flex column, space-between, padding 10px 12px（原上下紧凑布局）
  - `.shop-product-name`: 14px / 700（原 12px / 600）
  - 新增 `.shop-product-desc`: 11px, 2行截断
  - `.shop-product-meta`: 新增 margin-bottom: 6px
  - `.shop-product-price`: 18px（原 15px）
  - `.shop-product-cart-btn`: 28px（原 22px）, svg 14px（原 12px）
  - `.city-detail-products-grid .shop-product-card`: width 100%（原 50%）
- **JS 修改**:
  - `shop.js` `renderShopList()`: 移除首项特殊处理(idx===0)，所有卡片使用统一模板（含 desc + meta + bottom 三行布局）
  - `city.js` 同城详情卡片模板同步更新，新增 desc 行
- `--featured` 系列 CSS 变体保留未删（无引用，无副作用）
